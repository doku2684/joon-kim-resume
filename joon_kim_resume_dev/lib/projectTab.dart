import 'package:flutter/material.dart';

class ProjectTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('projectTab'),
      ),
    );
  }
}
